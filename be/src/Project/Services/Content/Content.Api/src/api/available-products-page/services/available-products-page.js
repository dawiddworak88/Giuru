'use strict';

/**
 * available-products-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::available-products-page.available-products-page');
