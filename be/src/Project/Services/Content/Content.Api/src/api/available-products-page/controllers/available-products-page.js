'use strict';

/**
 * available-products-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::available-products-page.available-products-page');
