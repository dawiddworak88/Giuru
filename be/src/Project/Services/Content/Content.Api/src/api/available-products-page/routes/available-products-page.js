'use strict';

/**
 * available-products-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::available-products-page.available-products-page');
